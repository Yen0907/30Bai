<?php
// Mảng cần kiểm tra
$array = array(5, 2, 8, 10, 3);

// Lọc các số lẻ
$oddNumbers = array_filter($array, function ($number) {
    return $number % 2 != 0;
});

// Tìm số lẻ lớn nhất
$largestOdd = max($oddNumbers);

echo "Số lẻ lớn nhất trong mảng là: $largestOdd";
?>